import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const index = createSlice({
    name: sliceName,
    initialState,
    reducers: {
    
    }
});

export const {

} = index.actions
export default index.reducer